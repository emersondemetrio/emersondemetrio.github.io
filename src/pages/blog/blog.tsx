
import "react";
import { Links } from "../../constants";

export const Blog = () => {
    console.log(Links);
    return (
        <section className="page-section" id="services">
            <div className="home-container">
                <h2>Blog</h2>
            </div>
        </section>
    );
};
